import styled from "styled-components"

export const ContainerCards = styled.div`
  display: flex;
  overflow: scroll;
  padding: 0 1.5rem;
  gap: 0.5rem;
  margin-bottom: 1.5rem;
  overflow: scroll;
`
